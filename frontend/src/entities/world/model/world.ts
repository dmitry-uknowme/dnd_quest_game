export interface World {
  id: string;
  title: string;
  description: string;
  genre: string;
  tone: string;
  global_goal: string;
  conflict_core: string;
  memory_seed: string;
  power_limits: string;
  world_rules: string;
  world_constraints: string;
}
